{
  "name": "tweak",
  "version": "1.0.0",
  "private": true,
  "description": "TWEAK — планировщик многоточечных путешествий с ИИ",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env.local dev-server.mjs",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vercel": "^59.1.3"
  }
}
